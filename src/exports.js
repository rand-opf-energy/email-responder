// This file is deployed as-is to Google Apps Script.
// It provides the statically analyzable top-level function declarations
// so that the Apps Script UI and time-driven triggers can find them.

function processEmailsTick() {
    return globalThis.processEmailsTick();
}

function installTrigger() {
    return globalThis.installTrigger();
}
